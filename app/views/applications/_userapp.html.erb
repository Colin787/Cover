<div class="table-responsive">
  <table class="table table-bordered">
    <thread>
      <tr>
        <th>Name</th>
        <th>Status</th>
        <th>Title</th>
        <th>Restaurant</th>
      </tr>
    </thread>
    <tbody>
      <td class="container"><%=userapp.user.first_name%></td>
      <td class="container"><%=userapp.job.jobtype.name%></td>
      <td class="container"><%=userapp.job.jobtype.name%></td>
      <td class="container"><%=userapp.job.user.restaurant_name%></td>
    </tbody>
  </table>
</div>