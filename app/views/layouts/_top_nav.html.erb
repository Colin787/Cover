<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <% if current_user %>
      <a class="navbar-brand" href="/jobs">Cover</a>
      <% else %>
      <a class="navbar-brand" href="/">Cover</a>
      <% end %>
    </div>
    <div id="navbar" class="navbar-collapse collapse">
      <% if current_user %>   
    <div class="col-sm-3 col-md-3">
      <%= form_tag(jobs_path, method: :get, class: "navbar-form") do %>
        <%= text_field_tag :search, params[:search], class: "form-control", placeholder: "Search" %>
      <% end %>
      </div>

      <ul class="nav navbar-nav navbar-right">
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
            You are Signed in, <%= current_user.first_name %>
            <span class="caret"></span>
            <ul class="dropdown-menu">
              <li><a href="/users/<%= current_user.id %>">Profile</a></li>
              <li><a href="/jobs">Job Posts</a></li>
              <% if current_user.usertype_id != 1 %>
              <li><a href="/users/<%= current_user.id %>/applications"> Applications </a></li>
              <% end %>

            </ul>
          <li><a href="/logout">Logout</a></li>
        </ul>
      <% else %>
       <div class="nav navbar-nav navbar-right">
          <a href="" class="btn btn-primary btn-xs signup" role="button" data-toggle="modal" data-target="#myModal">
          <strong> Sign up</strong>
          </a>

          <%= link_to 'Login', '/login', class: "btn btn-primary btn-xs login" %>
        </div>
      <% end %>
    </div>
  </div>
</nav>
<div id="myModal" class="modal fade" role="dialog">
  <div class=" text-center modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal">&times;</button>
        <h4 class="modal-title">Register as:</h4>
      </div>
      <div class="modal-body">
        <a href = "/users/worker" class="btn btn-primary worker" role="button">Worker</a> or
        <a href = "/users/restaurant" class="btn btn-primary restaurant" role="button">Restaurant Owner</a>
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
      </div>
    </div>
  </div>
</div>
